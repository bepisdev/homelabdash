<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= $page_title %></title>

  <!-- Bulma CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

  <!-- Base + Theme styles -->
	<link rel="stylesheet" href="/css/base.css">
		<link rel="stylesheet" href="/css/theme/<%= $config->{theme} || 'default' %>.css">
</head>
	<body class="theme-<%= $config->{theme}%>">
  <section class="section">
    <div class="container">
      <h1 class="title is-2"><%= $page_title %></h1>
			<h1 class="title is-2"><%= $config->{theme} || 'default' %></h1>

      % for my $section (@$sections) {
        <div class="card section-card">
          <header class="card-header">
            <h4 class="card-header-title">
              <%= $section->{title} %>
            </h4>
          </header>
          <div class="card-content">
            <div class="content">
							<% if($section->{widget} == "service_list") { %>
								<%= include '_service_list', services => $section->{content} %>
							<% } %>
            </div>
          </div>
        </div>
      % }
    </div>
  </section>
</body>
</html>
