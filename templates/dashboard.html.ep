<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= $page_title %></title>

  <!-- Bulma CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

  <!-- Theme styles -->
  <style>
		html, body {
			height: 100%;
			margin: 0;
		}

    body.theme-dark {
      background-color: #121212;
      color: #f0f0f0;
    }

    body.theme-hacker {
      background-color: #000000;
      color: #00ff88;
    }

    .theme-hacker a,
    .theme-hacker h1,
    .theme-hacker h2 {
      color: #00ff88;
    }

    .section-card {
      margin-bottom: 1rem;
    }
  </style>
</head>
	<body class="theme-<%= $config->{theme}%>">
  <section class="section">
    <div class="container">
      <h1 class="title is-2"><%= $page_title %></h1>
			<h1 class="title is-2"><%= $config->{theme} %></h1>

      % for my $section (@$sections) {
        <div class="card section-card">
          <header class="card-header">
            <h4 class="card-header-title">
              <%= $section->{title} %>
            </h4>
          </header>
          <div class="card-content">
            <div class="content">
              Placeholder content for section: <code><%= $section->{widget} || 'N/A' %></code>
            </div>
          </div>
        </div>
      % }
    </div>
  </section>
</body>
</html>
