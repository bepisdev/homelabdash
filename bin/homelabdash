#!/usr/bin/env perl

use strict;
use warnings;
use FindBin;
use lib "$FindBin::Bin/../lib";

use HomelabDash::Config;
use HomelabDash::Logger;
use HomelabDash::Server;

my $config = HomelabDash::Config::load_config("/config/dashboard.yml");
HomelabDash::Logger::init($ENV{'LOG_LEVEL'} || 'INFO');
